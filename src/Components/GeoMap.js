import React from 'react';
import {
    ComposableMap,
    Geographies,
    Geography,
    Sphere,
    Graticule
} from 'react-simple-maps';

const GeoMap = () => {
    return (
        <ComposableMap>

        </ComposableMap>
    );
};

export default GeoMap;
